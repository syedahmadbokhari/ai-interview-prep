# Optional. Default is "/api", which the Vite dev server (vite.config.js)
# and the nginx container both proxy to the FastAPI backend — same origin,
# no CORS needed. Only set this if the frontend is hosted separately from
# the API (which then needs CORS configured).
# VITE_API_BASE_URL=/api
